server:
  port: 9081

# 默认 dev：加载同目录下 application-dev.yaml（需本地自建，见 application-dev.example.yaml）
# 生产环境请设置环境变量 SPRING_PROFILES_ACTIVE=prod，并通过环境变量或 application-prod 提供密钥
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/openhsd?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://8.134.221.22:3306/openhsd?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
#    password: Cend123...
    password: huashidai123
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: openhsd-gateway-jwt-secret-key-2026-make-it-long-enough-256bit
  issuer: openhsd

minio:
  endpoint: http://huashidai1.com:9000
  access-key: aibox
  secret-key: aiboxminio
  bucket-name: aiworkbox-images
  external-url: https://www.huashidai1.com/oss

wechat:
  pay:
    app-id: ${WECHAT_APP_ID:your-app-id}
    mch-id: ${WECHAT_MCH_ID:your-mch-id}
    api-key: ${WECHAT_API_KEY:your-api-key}
    api-v3-key: ${WECHAT_API_V3_KEY:your-api-v3-key}
    notify-url: ${WECHAT_NOTIFY_URL:https://your-domain.com/api/recharge/wechat/notify}
    cert-path: ${WECHAT_CERT_PATH:}
    mock: ${WECHAT_MOCK:true}

openrouter:
  management-key: ${OPENROUTER_MANAGEMENT_KEY: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}
  base-url: https://openrouter.ai/api/v1

# 阿里云百炼语音识别 https://www.alibabacloud.com/help/zh/model-studio/real-time-speech-recognition
# API Key：application-dev.yaml（已 git 忽略）或环境变量 DASHSCOPE_API_KEY
dashscope:
  asr:
    enabled: false
    api-key: ${DASHSCOPE_API_KEY:}
    region: cn
    model: paraformer-realtime-v2
    default-format: aac
    default-sample-rate: 44100

